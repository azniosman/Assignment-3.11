apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "azni-app.fullname" . }}-test-connection"
  labels:
    {{- include "azni-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "azni-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
